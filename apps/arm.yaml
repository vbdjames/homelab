apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: arm
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: git@github.com:vbdjames/homelab.git
    targetRevision: HEAD
    path: infrastructure/arm

  destination:
    server: https://kubernetes.default.svc
    namespace: arm

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
