# Git aliases
alias ga='git add'
alias gcmsg='git commit -m'
alias gc='git commit'
alias gb='git branch'
alias gd='git diff'
alias gst='git status'

# History search — type partial command then Up/Down to cycle through matches
bind '"\e[A": history-search-backward'
bind '"\e[B": history-search-forward'
